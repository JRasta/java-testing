import java.util.Scanner;public class VolumeCalculation {    public static void main(String[] arg) {        Scanner reader = new Scanner(System.in);  // Reading from System.in        // Input from user        System.out.println("Enter the length of a side for a cube: ");        int cubeLength = reader.nextInt();        System.out.println("Enter the length of a side for a tetrahedron: ");        double tetrahedronLength = reader.nextInt();        System.out.println("Enter a radius for a sphere: ");        double radius = reader.nextInt();        reader.close();        // Output - unformatted        System.out.println(volume_of_cube(cubeLength));        System.out.println(volume_of_tetrahedron(tetrahedronLength));        System.out.println(volume_of_sphere(radius));    }    // Cube calculation    static int volume_of_cube(int cubeLength) {        return cubeLength * cubeLength * cubeLength;    }    // Some calculation calculation    static double volume_of_tetrahedron(double tetrahedronLength) {        return (Math.pow(tetrahedronLength, 3) / (6 * Math.sqrt(2)));    }    // Sphere calculation volume_of_cube    static double volume_of_sphere(double rad) {        return ((4.0 / 3.0) * (Math.PI * rad * rad * rad));    }}